import CoreGraphics

/// Layout metrics for the floating task panel.
enum PanelMetrics {
    static let panelSize = CGSize(width: 380, height: 480)
    static let cornerRadius: CGFloat = 18
    static let contentInset: CGFloat = 12
}
